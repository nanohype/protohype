node_modules/
dist/
cdk.out/
.next/
.env
*.log
.DS_Store

# Filled-in secret files — seed with `npm run seed:{env}` then leave the file
# on your laptop. Template (committed) is secrets.template.json.
dispatch-secrets.*.json
